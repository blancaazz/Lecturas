language: node_js

build:
    ci:
        - docker build -t cont_lecturas .
        - docker run -t -v $SHIPPABLE_BUILD_DIR:/test cont_lecturas
        #- docker run -t -v $SHIPPABLE_BUILD_DIR:/test blancaazz/lecturas
    # pre_ci_boot:
    #     image_name: blancaazz/lecturas
    #     image_tag: latest
    #     pull: true
    #     env: FOO=bar
    #     options: "-e HOME=/ -t -v $SHIPPABLE_BUILD_DIR:/test blancaazz/lecturas"
        